The code is designed for a 360-degree servo motor, but it's very easy to modify for a standard 180-degree servo motor. Simply adjust `doorLock.write(value);` to the desired angle based on your needs.
